#include <stdio.h>

int main(){

// 13) Explique as diferenças entre as instruções de repetições while e for

/* Resposta:

A estrutura de repetição "for" é utilizada quando se quer executar um bloco
de comandos um número de vezes previamente determinado.
Exemplo: Execute um comando 10 vezes / imprima um valor 30 vezes / leia 10 valores, etc.

Já a estrutura de repetição "while" é indicada quando o programa deve ser executado uma
quantidade indeterminada de vezes, uma vez que o usuario deve efetuar o encerramento do
comando, o que geralmente acontece com situações que envolvam FLAG.
Exemplo:  Execute um programa que leia uma quantidade não determinada de números; quando o
usuario digitar "0" o programa sera encerrado.

*/
return 0;
}
